#include <stdio.h>

int main(void){
    int counts[] = {1, 3, 5, 6, 5, 2};
    for (int i = 0; i < 6 ; i++){
        printf({%box(①,6)%});
        for (int j = j =0; j < {%box(②,6)%}; j++){
            printf("*");
        }
        printf("\n");
    }
    return 0;
}